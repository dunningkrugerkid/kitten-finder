services:
  kitten-finder:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/kitten-finder.db
    volumes:
      - kitten-data:/app/data
    restart: unless-stopped

volumes:
  kitten-data:
